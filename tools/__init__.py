"""One-off tools: evidence collection, benchmarks, and research scripts.

A package rather than loose scripts so that the parsing helpers here can be imported by the
real-evidence tests without the same file being seen under two module names. Nothing in the
production package imports from here.
"""
